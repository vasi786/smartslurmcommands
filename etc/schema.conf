# Document supported keys, types, and defaults
# key,type,default,description
color,enum(auto|always|never),auto,Color output mode
confirm,bool,true,Ask for confirmation on destructive ops
dry_run,bool,false,Print commands instead of executing
log_level,enum(debug|info|warn|error),info,Log verbosity
timeout,duration,30s,External command timeout
parallelism,int,8,Concurrency level
smartcancel.scope,enum(mine|user:<uid>|account:<acct>|partition:<p>|state:<state>),mine,Default cancel scope
smartcancel.grace_period,duration,5s,Wait before sending signals
smartcancel.reason_prefix,string,smart,Prefix for scancel reason
