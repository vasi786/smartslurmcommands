# Global defaults
color=auto
confirm=true
dry_run=false
log_level=info
timeout=30s
parallelism=8

# Per-tool defaults
smartcancel.scope=mine
smartcancel.grace_period=5s
smartcancel.reason_prefix=smart
