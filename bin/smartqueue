#!/usr/bin/env bash
# Thin shim that executes cmd/smartqueue/smartqueue.sh
set -Eeuo pipefail
DIR="$(cd "$(dirname "$0")/.." && pwd)"
export SSC_HOME="$DIR"
exec "$DIR/cmd/smartqueue/smartqueue.sh" "$@"
